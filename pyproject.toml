[project]
name = "election-ai"
version = "0.2.0"
description = "Plataforma probabilística com modelo eleitoral Bayesiano hierárquico"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
